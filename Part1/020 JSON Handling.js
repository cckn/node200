const user = {name : 'ds', age : 29}

console.log(`user : `, user)
console.log(`user.name : ${user.name}`)
console.log(`user.age : ${user.age}`)


user.job = 'programmer'
user.nation = 'korea'
console.log(user)

const membername = 'age'
console.log(user[membername])