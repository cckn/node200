const num1 = 1
const num2 = 2
const list = [1,2,3,4]
const emptyList = []

const result = num1 > num2 ? 'num1' : 'num2'

console.log(`${result}가 더 크다.`)

list.length > 0 ? console.log(list) : console.log('list is empty')
emptyList.length > 0 ? console.log(list) : console.log('list is empty')

